# Production-only deps: exactly what server.py imports.
# Excludes emergentintegrations (not on public PyPI - breaks the build),
# and dev/test tooling (pytest, black, flake8, mypy) plus unused libs
# (boto3, pandas, numpy, jq, typer) that only slow the deploy down.
fastapi==0.110.1
uvicorn==0.25.0
motor==3.3.1
pymongo==4.6.3
pydantic>=2.6.4
email-validator>=2.2.0
python-dotenv>=1.0.1
bcrypt==4.1.3
pyjwt>=2.10.1
